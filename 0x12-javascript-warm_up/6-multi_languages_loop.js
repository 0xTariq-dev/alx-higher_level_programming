#!/usr/bin/node
// Script to iterrate over array elements and print them.
const strings = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
strings.forEach((element) => { console.log(element); });
